<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @group.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @group.description %>
</p>

<p>
  <strong>Private:</strong>
  <%= @group.private %>
</p>

<p>
  <strong>User:</strong>
  <%= @group.user.email %>
</p>

<!-- <p><%= @group.to_json(:include => {:lists => {:include => :tasks} }) %></p> -->
<%= tag.div id: :board, data: { board: @group.to_json(:include => {:lists => {:include => :tasks} }) } %>

<br>
<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', groups_path %>
